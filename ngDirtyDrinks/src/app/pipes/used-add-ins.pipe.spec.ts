import { UsedAddInsPipe } from './used-add-ins.pipe';

describe('UsedAddInsPipe', () => {
  it('create an instance', () => {
    const pipe = new UsedAddInsPipe();
    expect(pipe).toBeTruthy();
  });
});
